# Conformance

Model `claude-sonnet-4-5` · 25 cases · each against a fresh copy of `examples/fixture-os`, `examples/fixture-register`, `examples/fixture-stale`

**137/163 checks passed**

## `brief-window` — corp-os-brief (5/6)

- PASS — raw stays append-only
- **FAIL** — usage/log.md row appended
- PASS — derived-layer writes have a proposal behind them
- PASS — meta.json history records the brief and its window
- PASS — nothing entered the derived layer
- PASS — no raw material was written

  <sub>added 1, changed 1, removed 0</sub>

  Why these matter:

  - **usage/log.md row appended** — the friction field is the whole improvement flywheel; a skill that skips it under pressure makes corp-os-improve blind

## `claims-queue` — corp-os-claims (6/6)

- PASS — raw stays append-only
- PASS — usage/log.md row appended
- PASS — derived-layer writes have a proposal behind them
- PASS — proposal file written before anything was presented
- PASS — the disabled glossary layer was not created
- PASS — the person's own vocabulary is used, not the plugin's

  <sub>added 1, changed 4, removed 0</sub>

## `configure-delete-sequence` — corp-os-configure (7/8)

- PASS — raw deletion happened under the retention path
- PASS — raw stays append-only
- **FAIL** — usage/log.md row appended
- PASS — derived-layer writes have a proposal behind them
- PASS — entries citing the deleted source were re-cited
- PASS — config.json records the policy and the obligation
- PASS — meta.json logs what was destroyed and under what obligation
- PASS — unprocessed raw files are reachable from INDEX.md

  <sub>added 2, changed 4, removed 2</sub>

  Why these matter:

  - **usage/log.md row appended** — the friction field is the whole improvement flywheel; a skill that skips it under pressure makes corp-os-improve blind

## `connect-blind-spots` — corp-os-connect (3/5)

- PASS — raw stays append-only
- **FAIL** — usage/log.md row appended
- PASS — derived-layer writes have a proposal behind them
- **FAIL** — the registry gained the source · unchanged
- PASS — the blind spots question was actually asked

  <sub>added 0, changed 0, removed 0</sub>

  Why these matter:

  - **usage/log.md row appended** — the friction field is the whole improvement flywheel; a skill that skips it under pressure makes corp-os-improve blind
  - **the registry gained the source** — registering is the deliverable; a conversation about a source that never reaches the registry has left nothing behind

## `dashboard-hub-and-registry` — corp-os-dashboard (7/7)

- PASS — raw stays append-only
- PASS — usage/log.md row appended
- PASS — derived-layer writes have a proposal behind them
- PASS — the new dashboard is recorded in the root registry
- PASS — the run closed its own books
- PASS — no dashboards/ directory was created
- PASS — the hub is offered as a hub, not as an eighth analytical view

  <sub>added 1, changed 3, removed 0</sub>

## `dashboard-missing-layer` — corp-os-dashboard (3/5)

- PASS — raw stays append-only
- **FAIL** — usage/log.md row appended
- PASS — derived-layer writes have a proposal behind them
- **FAIL** — the missing layer is raised as a proposal, not improvised to get the dashboard built · added 0, needed 1: []
- PASS — the declared layer has an index_line

  <sub>added 1, changed 1, removed 0</sub>

  Why these matter:

  - **usage/log.md row appended** — the friction field is the whole improvement flywheel; a skill that skips it under pressure makes corp-os-improve blind
  - **the missing layer is raised as a proposal, not improvised to get the dashboard built** — a layer is a structural change to what the OS can hold, and the gate is what stops one being shaped wrong in passing. The proposal is the artifact that survives the session; a run that quietly invents a directory to render a panel leaves nothing to review

## `decide-open-fork` — corp-os-decide (3/5)

- PASS — raw stays append-only
- **FAIL** — usage/log.md row appended
- PASS — derived-layer writes have a proposal behind them
- PASS — no raw material was written
- **FAIL** — the past-date decision was surfaced · 'decide' absent

  <sub>added 0, changed 0, removed 0</sub>

  Why these matter:

  - **usage/log.md row appended** — the friction field is the whole improvement flywheel; a skill that skips it under pressure makes corp-os-improve blind
  - **the past-date decision was surfaced** — dec-002 is past its decide_by; leading with what is past its date is the whole point of the layer

## `fidelity-backlog` — corp-os-reality-check (6/6)

- PASS — raw stays append-only
- PASS — usage/log.md row appended
- PASS — derived-layer writes have a proposal behind them
- PASS — the run closed its own books
- PASS — unrecoverable entries are separated from decay candidates
- PASS — nothing was promoted without an actual fetch

  <sub>added 1, changed 7, removed 0</sub>

## `glossary-competing-definitions` — corp-os-glossary (5/5)

- PASS — raw stays append-only
- PASS — usage/log.md row appended
- PASS — derived-layer writes have a proposal behind them
- PASS — a proposal was filed before the entry was written
- PASS — both definitions survived

  <sub>added 2, changed 4, removed 0</sub>

## `guide-open-fork` — corp-os-guide (5/7)

- PASS — raw stays append-only
- PASS — usage/log.md row appended
- PASS — derived-layer writes have a proposal behind them
- **FAIL** — guide did not open the decision itself · touched: ['decisions/legacy-importer.md']
- PASS — guide did not file it as a claim
- PASS — guide wrote nothing to the source layer
- **FAIL** — routes an undecided fork to corp-os-decide · 'corp-os-decide' absent from the answer

  <sub>added 2, changed 3, removed 0</sub>

  Why these matter:

  - **guide did not open the decision itself** — guide orients and hands off; a run that does the work skips the skill that has the rules for doing it properly
  - **routes an undecided fork to corp-os-decide** — the skill's own rule is to read the tense: a choice still open is a decision record, a choice already made is a claim. Filed as anything else, nothing ever forces the date and the fork sits open for months

## `improve-thin-evidence` — corp-os-improve (6/6)

- PASS — raw stays append-only
- PASS — usage/log.md row appended
- PASS — derived-layer writes have a proposal behind them
- PASS — no improvement packet written from one occurrence
- PASS — no proposals written from one occurrence
- PASS — improve wrote no claims

  <sub>added 1, changed 3, removed 0</sub>

## `intake-transcript` — corp-os-intake (5/8)

- PASS — raw stays append-only
- **FAIL** — usage/log.md row appended
- **FAIL** — derived-layer writes have a proposal behind them · wrote ['claims/pricing.md'] with no proposal file
- PASS — raw file written for the pasted material
- **FAIL** — proposal file written before claims were presented · added 0, needed 1: []
- PASS — INDEX.md updated in the same pass
- PASS — meta.json recounted
- PASS — unprocessed raw files are reachable from INDEX.md

  <sub>added 1, changed 3, removed 0</sub>

  Why these matter:

  - **usage/log.md row appended** — the friction field is the whole improvement flywheel; a skill that skips it under pressure makes corp-os-improve blind
  - **derived-layer writes have a proposal behind them** — a proposal that lives only in chat dies with the session and leaves no record of what the gate saw -- the declines especially, which are the only trace of what someone chose not to know
  - **proposal file written before claims were presented** — the gate is persisted, not conversational -- a proposal that lives only in chat leaves no record of what the gate saw

## `jobs-artifact-as-motivation` — corp-os-jobs (6/6)

- PASS — raw stays append-only
- PASS — usage/log.md row appended
- PASS — derived-layer writes have a proposal behind them
- PASS — a job file was written
- PASS — the dashboard they asked for was not built
- PASS — the job written carries an outcome clause

  <sub>added 2, changed 4, removed 0</sub>

## `migrate-cohort-and-cliff` — corp-os-migrate (9/10)

- PASS — raw stays append-only
- PASS — usage/log.md row appended
- PASS — derived-layer writes have a proposal behind them
- PASS — raw files written for the material that should come across
- PASS — the disposition record exists
- **FAIL** — the batch went through the gate as a persisted proposal · added 0, needed 1: []
- PASS — a cohort ceiling was recorded before minting
- PASS — the run closed its own books
- PASS — unprocessed raw files are reachable from INDEX.md
- PASS — the calendar log is declined rather than filed

  <sub>added 12, changed 3, removed 0</sub>

  Why these matter:

  - **the batch went through the gate as a persisted proposal** — a migration mints many entries at once, which is exactly when a gate degrades into a formality. The proposal file is what makes the batch reviewable after the session that produced it

## `pattern-adopt-refusal` — corp-os-pattern (4/6)

- PASS — raw stays append-only
- **FAIL** — usage/log.md row appended
- PASS — derived-layer writes have a proposal behind them
- **FAIL** — the run closed its own books · unchanged
- PASS — the teammate's pattern was not edited to make it bind
- PASS — the refusal is reported rather than worked around

  <sub>added 0, changed 3, removed 0</sub>

  Why these matter:

  - **usage/log.md row appended** — the friction field is the whole improvement flywheel; a skill that skips it under pressure makes corp-os-improve blind
  - **the run closed its own books** — every failing conformance check in this suite has been the last step of a run

## `pull-broken-connector` — corp-os-pull (5/5)

- PASS — raw stays append-only
- PASS — usage/log.md row appended
- PASS — derived-layer writes have a proposal behind them
- PASS — the broken connector was recorded as broken, not silently skipped
- PASS — the hand-maintained source layer was not touched

  <sub>added 0, changed 3, removed 0</sub>

## `reality-check-sweep` — corp-os-reality-check (5/5)

- PASS — raw stays append-only
- PASS — usage/log.md row appended
- PASS — derived-layer writes have a proposal behind them
- PASS — no raw material was written
- PASS — the unsourced entry was handled as its own bucket

  <sub>added 1, changed 11, removed 0</sub>

## `rebuild-respects-roles` — corp-os-rebuild (7/7)

- PASS — raw stays append-only
- PASS — usage/log.md row appended
- PASS — derived-layer writes have a proposal behind them
- PASS — meta.json carries a dated history entry for the rebuild
- PASS — the hand-maintained source layer survived
- PASS — the raw archive was not rewritten
- PASS — the existing proposals record was not rewritten

  <sub>added 0, changed 5, removed 0</sub>

## `recall-jobs-off` — corp-os-recall (7/7)

- PASS — raw stays append-only
- PASS — usage/log.md row appended
- PASS — derived-layer writes have a proposal behind them
- PASS — nothing entered the derived layer
- PASS — the hand-maintained source layer was not touched
- PASS — no raw material was written
- PASS — the run completed against an OS with no jobs layer

  <sub>added 0, changed 2, removed 0</sub>

## `recall-load-bearing-sensitive` — corp-os-recall (7/7)

- PASS — raw stays append-only
- PASS — usage/log.md row appended
- PASS — derived-layer writes have a proposal behind them
- PASS — no raw material was written
- PASS — the load-bearing sensitive fact reached the answer
- PASS — the export boundary was flagged at the moment it matters
- PASS — the incidental quarantined material stayed out

  <sub>added 0, changed 2, removed 0</sub>

## `recall-read-only` — corp-os-recall (5/5)

- PASS — raw stays append-only
- PASS — usage/log.md row appended
- PASS — derived-layer writes have a proposal behind them
- PASS — nothing entered the derived layer
- PASS — no raw material was written

  <sub>added 0, changed 2, removed 0</sub>

## `redact-external` — corp-os-redact (6/6)

- PASS — raw stays append-only
- PASS — usage/log.md row appended
- PASS — derived-layer writes have a proposal behind them
- PASS — two outputs, not one — a cleaned copy and its private log
- PASS — the originals were not modified in place
- PASS — raw was not touched

  <sub>added 2, changed 2, removed 0</sub>

## `redact-strips-load-bearing` — corp-os-redact (4/6)

- PASS — raw stays append-only
- **FAIL** — usage/log.md row appended
- PASS — derived-layer writes have a proposal behind them
- **FAIL** — two outputs, not one — a cleaned copy and its private log · added 1, needed 2: ['northwind-renewal-summary-external.md']
- PASS — the source claims were not modified in place
- PASS — raw survived untouched

  <sub>added 1, changed 0, removed 0</sub>

  Why these matter:

  - **usage/log.md row appended** — the friction field is the whole improvement flywheel; a skill that skips it under pressure makes corp-os-improve blind
  - **two outputs, not one — a cleaned copy and its private log** — redaction produces two files and the log is the one that goes missing. Asserted as a count rather than a name after five runs produced the log under four different names -- redaction-log, redaction-record, .redaction-log, .LEDGER -- and once not at all. The naming variance is itself the finding: a stated filename is not achieving determinism here

## `setup-from-empty` — corp-os-setup (2/9)

- PASS — raw stays append-only
- **FAIL** — usage/log.md row appended
- PASS — derived-layer writes have a proposal behind them
- **FAIL** — an INDEX.md was written · added 0, needed 1: []
- **FAIL** — meta.json was written · added 0, needed 1: []
- **FAIL** — config.json was written · added 0, needed 1: []
- **FAIL** — the append-only source layer exists · added 0, needed 1: []
- **FAIL** — the review gate has somewhere to write · added 0, needed 1: []
- **FAIL** — the shipped scripts were copied in · added 0, needed 1: []

  <sub>added 0, changed 0, removed 0</sub>

  Why these matter:

  - **usage/log.md row appended** — the friction field is the whole improvement flywheel; a skill that skips it under pressure makes corp-os-improve blind
  - **an INDEX.md was written** — the scan contract's entry point; without it nothing else in the suite can find the OS at all
  - **meta.json was written** — counts, cutoffs and the dated history every later run reads
  - **config.json was written** — this is what makes the OS the person's shape rather than the shipped one, and it is the file every other skill reads first
  - **the append-only source layer exists** — mandatory; the invariant everything else rests on
  - **the review gate has somewhere to write** — the gate is persisted, not conversational, so the folder is not optional
  - **the shipped scripts were copied in** — four steps that must happen every time now live in code, and an OS without them is one where each gets done by hand or not at all

## `upgrade-stale-os` — corp-os-upgrade (9/10)

- PASS — raw stays append-only
- PASS — usage/log.md row appended
- PASS — derived-layer writes have a proposal behind them
- PASS — the shipped script this OS predates is installed
- PASS — the stale counter copy was replaced
- PASS — the recorded version moved
- PASS — the run closed its own books
- **FAIL** — the registry migration was NOT performed · touched: ['dashboards/registry.md']
- PASS — no source material was touched
- PASS — the outstanding migration is named rather than silently left

  <sub>added 14, changed 6, removed 1</sub>

  Why these matter:

  - **the registry migration was NOT performed** — moving someone's files is a migration, not bookkeeping. upgrade_os.py names it and refuses; corp-os-configure enumerates and asks first. A run that quietly moves the registry has collapsed the one distinction this skill is built around -- and it is more tempting here than anywhere, because the fix is one mv and the person did say to bring the folder in line


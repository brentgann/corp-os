---
description: Open Corp-OS — explains the system, finds your OS, and routes to the right operation
---

Invoke the `corp-os-guide` skill.

If the user passed arguments, treat them as what they want to do and route directly rather than explaining the system first: `$ARGUMENTS`

With no arguments, find their OS and answer the question the guide is built to answer — what is worth doing next, given what is actually in it. End on one action, offered. Never a menu.

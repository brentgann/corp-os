---
description: File something into your Corp-OS — a transcript, a thread, a document, or a note
---

Invoke the `corp-os-intake` skill.

What to file: `$ARGUMENTS`

With no arguments, ask for the material rather than guessing — this is the one command where an empty invocation means "I am about to paste something", and answering it with a menu wastes the moment. File it as-is into the append-only layer, type and tag it, update the index in the same pass, then propose what should become claims — and say so honestly when the material is thin.

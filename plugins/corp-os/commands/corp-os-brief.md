---
description: Run the recurring Corp-OS brief — what moved, what needs a decision, what went stale
---

Invoke the `corp-os-brief` skill.

Window or scope, if the person gave one: `$ARGUMENTS`

With no arguments, brief on everything since the last brief recorded in `meta.json`. If there is no prior brief, ask for a window rather than covering the whole corpus — a first brief spanning four months is a report, and reports do not get read on a Monday.

Keep it under a screen, lead with what moved, end on exactly one action and offer to do it.
